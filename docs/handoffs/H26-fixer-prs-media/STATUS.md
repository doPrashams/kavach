# H26 — Fixer PRs + media

**GITHUB_PAT:** not present in shell env or `backend/.env` / `deploy/.env` during this
pass — skipped opening additional Fixer PRs against `kavach-demo-pipeline`.

**Existing live PR:** [#1 value_corruption (merged)](https://github.com/doPrashams/kavach-demo-pipeline/pull/1)

**Dry-run artifacts:** `examples/prs/{schema_drift,null_spike,freshness_lag,value_corruption}/`

**Media:** placeholders under `docs/media/` (GIF/screenshot capture deferred to video day).
