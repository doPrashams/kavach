# Media placeholders

Screenshots and GIFs for the Devpost / video day are **pending**.

Record per [`docs/VIDEO.md`](../VIDEO.md):

- War room wide shot
- Chaos inject + SSE agent feed
- Blast-radius / lineage graph
- Fixer PR card
- MTTR before/after chart
- Replay mode (zero keys)

Drop assets here as `war-room.gif`, `mttr-chart.png`, etc. Prefer linking from
`docs/VIDEO.md` / Devpost rather than committing large binaries until capture day.
